export class Vendor {
    vendor_id !: number;
	vendor_name !: string;
	vendor_email !: string;
	vendor_contact !: number;
	state_name !: string;
    city_name !: string;
	pincode !: string;
	vendor_gst !: string;
	vendor_address !: string;
}